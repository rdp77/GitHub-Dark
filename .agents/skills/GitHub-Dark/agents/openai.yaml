interface:
  display_name: "GitHub Dark"
  short_description: "Repo-specific patterns and workflows for GitHub-Dark"
  default_prompt: "Use the GitHub-Dark repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true